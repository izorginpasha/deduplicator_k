services:
  redis:
    container_name: redis
    image: redis:7
    ports:
      - "6379:6379"
    networks:
      - app-network

  kafka:
    container_name: kafka
    image: bitnami/kafka:latest
    ports:
      - "9092:9092"
    env_file:
      .env
    depends_on:
      - redis
    networks:
      - app-network

  clickhouse:
    container_name: clickhouse
    image: clickhouse/clickhouse-server:latest
    ports:
      - "8123:8123"   # HTTP-интерфейс
      - "9000:9000"   # TCP-интерфейс для клиентов
    env_file:
      .env
    ulimits:
      nofile:
        soft: 262144 #текущий лимит
        hard: 262144 # потолок
    volumes:
      - clickhouse_data:/var/lib/clickhouse
      - ./clickhouse-init-scripts:/docker-entrypoint-initdb.d  # Добавляем путь к скриптам
    networks:
      - app-network

  api:
    container_name: api
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    volumes:
      - .:/app
    env_file:
      .env
    depends_on:
      - kafka
      - redis
      - clickhouse
    networks:
      - app-network
    command: uvicorn api.main:app --host 0.0.0.0 --port 8000 --reload

  consumer:
    build:
      context: .
      dockerfile: Dockerfile
    working_dir: /app
    command: python consumer/consumer.py
    volumes:
      - .:/app
    env_file:
      .env
    depends_on:
      - kafka
      - redis
      - clickhouse
    networks:
      - app-network

networks:
  app-network:
    driver: bridge

volumes:
  clickhouse_data:
